# Icons (SVG)

Dépose tes icônes SVG dans ce dossier.

## Utilisation dans le renderer

- Exemple en HTML:

```html
<img src="/icons/settings.svg" alt="Settings" />
```

- Exemple en JS:

```js
const img = document.createElement('img');
img.src = '/icons/settings.svg';
img.alt = 'Settings';
```


